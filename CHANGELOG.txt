9.9.2021 
VARDICT rule yaml - problemy s verzi 1.7 out of nothing, upgrade na 1.8.2 (upravuje nejake dalsi chyby, je v snakemake wrappers), funguje?
VARDICT rule wrapper  - vymazal jsem "2> logfile" z pomocneho skriptu wrapperu ktere se stejne nepouzivali, vardict nema skoro zadny logovani... Pripadne zkontrolovat. 

postprocess_somaticseq_variants.R  - pridany IF  ELSE vyjimky pokud je Consensus INDEL prazdny. Stalo se u PCA_116 z datasetu PCA-E48. 

10.9.2021
process_and_format_annot_variants.R - opravena chybejici promenna unformated_sample_tab - nahrazeno za sample_tab, nove se zapisou prazdne soubory pro samply bez nalezenych variant

14.6.2021
STRELKA rule wrapper - po dokonceni skriptu se smaze slozka workspace
                        #shell("rm -fR " + snakemake.params.dir + "/workspace")

16.9.2021                        
postprocess_somaticseq_variants.R - pridano class(location_tab$chrom) = "character" do funkce "annotate_with_intervals"
                        # MODIFIED 16.9.2021 - datatype checked to character, when only numeric chromosomes are listed, its fread as integer
                        # Incompatible join types: x.chrom (character) and i.chrom (integer)

########################################################################################################################
13.12.2021 vetev SEKVOJA

Snakefile - sample_tab upraven do puvodni podoby z noveho JSONU
callers.smk - upravena input funkce pro mutect headers (stejne jako sample_names)
dalsi pravidla - JSON nove
    calling_type > "is_paired",
    "reference", stejne
    library_scope > "lib_ROI"

########################################################################################################################
LEDEN 2022
GLOBAL_REF_PATH = "/mnt/references/"   misto mnt/ssd3/references - kvuli Dockeru

config["calling_type"]  zmeneno na  config["is_paired"]
ve wrapperech zmeneno   snakemake.params.calling_type=="paired" na snakemake.params.calling_type:
"../input_files/mapped/ "  zmeneno na "mapped/"


POZNAMKY:
single sample - bude chyba v R skriptech
is_paired - jak se nastavuje? je required - zdedeno z alignment

JSON values not used? or required:

config["material"] - nastaveno ve SnakeFile napevno na "DNA"
config["not_use_merged"] - ve workflow.config.json na False by default

config["organism"] - based on libROI
config["reference"] - based on libROI

slaby_children_soma.txt zmeneno /mnt/ssd/ssd_3 na mnt/references
OVERIT ##### Reference processing

13.2.2022 FIRST RELEASE